<!DOCTYPE html>
<html dir="ltr" lang="en-US">
<% include ./partials/head %>
<style>
#sugaritem {
  margin:4px;
  text-align: center;
  background-color:#f7f7f7;
  border-radius:14px;
  border:1px solid #D0D0D0;
  overflow:auto;
  float:left;
  color:#1f93ff;
  width: 14.0em;
  cursor: move;
}
#sugaritemgreen {
  margin:4px;
  text-align: center;
  background-color:#f7f7f7;
  border-radius:14px;
  border:1px solid #D0D0D0;
  overflow:auto;
  float:left;
  color:green;
  width: 14.0em;
  cursor: move;
}
#sugaritemblue {
  margin:4px;
  text-align: center;
  background-color:#f7f7f7;
  border-radius:14px;
  border:1px solid #D0D0D0;
  overflow:auto;
  float:left;
  color:blue;
  width: 14.0em;
  cursor: move;
}

</style>


<body>

<ol id="slippylist">
  <li id="sugaritemgreen" class="no-reorder">Most Natural</li>
  <% for(var i=0; i<list.length; i++) {%>

      <li id="sugaritem"><%= list[i] %></li>

  <% } %>
  <li id="sugaritemblue" class="no-reorder">Most Artificial</li>
</ol>

<script>

window.extAsyncInit = function() {
  MessengerExtensions.getUserID(function success(uids) {
       var psid = uids.psid.toString();
       $("#facebook_id").val(psid)
     }, function error(err) {

     });
};

function setupSlip(list) {

  list.addEventListener('slip:beforeswipe', function(e){e.preventDefault();}, false);

  list.addEventListener('slip:beforereorder', function(e){
            if (e.target.classList.indexOf('no-reorder')) {
                e.preventDefault();
            }
        }, false);

  list.addEventListener('slip:reorder', function(e){
        e.target.parentNode.insertBefore(e.target, e.detail.insertBefore);
        return false;
  }, false);

  return new Slip(list);

}

setupSlip(document.getElementById('slippylist'));

var startX, startY, endX, endY;
document.addEventListener("touchmove", function(e){
    pageX = e.touches[0].pageX;
    pageY = e.touches[0].pageY;

    window.scroll(pageX, pageY);
}, false);


</script>
</body>
